<Window x:Class="HardwareMonitorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HardwareMonitorApp"
        mc:Ignorable="d"
        Title="Hardware Monitor" Height="500" Width="380"
        SizeToContent="WidthAndHeight" MinWidth="320">
    
    <Window.Resources>
        <!-- Common styles -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="5" />
        </Style>
        
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
        </Style>
        
        <Style x:Key="SectionStyle" TargetType="GroupBox">
            <Setter Property="Margin" Value="0,5" />
        </Style>
        
        <!-- Item template for lists -->
        <DataTemplate x:Key="KeyValueTemplate">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Key}" Style="{StaticResource LabelStyle}" Margin="5,0" />
                <TextBlock Grid.Column="1" Text="{Binding Value}" Style="{StaticResource ValueStyle}" Margin="5,0" />
            </Grid>
        </DataTemplate>
    </Window.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Top Control Panel -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            
        </StackPanel>
        
        <!-- Views Container -->
        <Grid Grid.Row="1">
            <!-- Loading View -->
            <Grid x:Name="LoadingView" Visibility="Visible">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Hardware Monitor" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Initializing hardware sensors..." FontSize="16" Margin="0,20,0,0" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="AdminStatusText" Text="Running with administrator privileges" FontSize="12" Margin="0,10,0,0" 
                               Foreground="Green" HorizontalAlignment="Center" FontStyle="Italic"/>
                    <ProgressBar IsIndeterminate="True" Width="300" Height="20" Margin="0,20,0,0"/>
                </StackPanel>
            </Grid>
            
            <!-- Numeric Dashboard View -->
            <Grid x:Name="NumericView" Visibility="Collapsed">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- CPU Section -->
                        <GroupBox Header="CPU" Style="{StaticResource SectionStyle}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="Temp:" Style="{StaticResource LabelStyle}" />
                                    <TextBlock Grid.Column="1" x:Name="CpuTempText" Style="{StaticResource ValueStyle}" />
                                </Grid>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="Load:" Style="{StaticResource LabelStyle}" />
                                    <TextBlock Grid.Column="1" x:Name="CpuLoadText" Style="{StaticResource ValueStyle}" />
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                        
                        <!-- GPU Section -->
                        <GroupBox Header="GPU" Style="{StaticResource SectionStyle}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="Temp:" Style="{StaticResource LabelStyle}" />
                                    <TextBlock Grid.Column="1" x:Name="GpuTempText" Style="{StaticResource ValueStyle}" />
                                </Grid>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="Load:" Style="{StaticResource LabelStyle}" />
                                    <TextBlock Grid.Column="1" x:Name="GpuLoadText" Style="{StaticResource ValueStyle}" />
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                        
                        <!-- Memory Section -->
                        <GroupBox Header="Memory" Style="{StaticResource SectionStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" Text="Temp:" Style="{StaticResource LabelStyle}" />
                                <TextBlock Grid.Column="1" x:Name="MemoryTempText" Style="{StaticResource ValueStyle}" />
                            </Grid>
                        </GroupBox>
                                                
                        <!-- Disk Section -->
                        <GroupBox Header="Disk Temperatures" Style="{StaticResource SectionStyle}">
                            <ItemsControl x:Name="DiskTempsItemsControl" ItemTemplate="{StaticResource KeyValueTemplate}" />
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>